# @@@@@@@@@@@@@
# CS1400 - MW1
# Assignment ???

import pattern
import turtle

pattern.setup()
#def drawRectanglePattern(centerX, centerY, offset, width, height, count, rotation):
#pattern.drawCirclePattern(centerX, centerY, offset, radius, count)
pattern.drawCirclePattern(0, 0, -50, 75, 30)

turtle.done()
