def rows(numberOfRows):
    numberOfRows = ""
    for rows in range(0, int(numberOfRows)):
        rowOfText = " "
        for currentRowNumber in range(0, rows + 1):
            if currentRowNumber == rows.length():
                print()
print(rows())
        
        def rows(numberOfRows):
    numberOfRows = ""
    for rows in range(0, int(numberOfRows)):
        rowOfText = " "
        for currentRowNumber in range(0, rows + 1):
            if currentRowNumber == rows.length():
                print()
print(rows())
        
        
