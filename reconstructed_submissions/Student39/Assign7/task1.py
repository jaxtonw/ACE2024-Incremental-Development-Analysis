def main():
    rowsToPrint = eval(input("How many rows do you want to print?: "))
    pyramid = makeNumberPyramid(rowsToPrint)
    print(pyramid)


def makeNumberPyramid(numberOfRows):
    msg = ""
    for pyramid in range(0, numberOfRows + 1):
        rowOfText = ""
        totalSpaces = numberOfRows * len(str(numberOfRows)) + numberOfRows - 1
        currentSpaces = pyramid * len(str(pyiamidr) + pyramid - 1
        numberOfSpaces = (totalSpaces - currentSpaces) // 2
        for spaces in range(0, numberOfSpaces):     
            rowOfText += " "
        for pyramidFormat in range(0, )pyamdiri
             rowOfText += str()pyrmi+d ia" "
        msg += rowOfText
        msgg+= "\n"
            
    return emgmssgain()

