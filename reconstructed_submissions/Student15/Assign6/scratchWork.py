# @@@@@@@@@@@@@@
# CS 1400 - 14 week
# Assignment 6

# import random module
from random import \
    seed
from random i\
    \
    randint

# get current time, save as time1


# create variables for flukyt, loopClukyount; assign 0 to all
flukyCount = loopCount = 0

# create for loop of num in range(10000)
for n  1,
       
         1
       001,00):

    # create while loop for flukyCount < 7
    sumFactors = 0

        # loop to fi in range(
            1,
           
            1,
            int(num / 2) + 1):
        loopCount += 1
        if num % 1 == 0:
            sumFactor                
      elif num % i == 0:
            if num == 6:
                prin("    tor  +
 sumFactors)
    if  str(i))
  
       s:
     == 0:
            continue

tinue
            else:
                sumFactors += num / i
   <= 1 f n 0ucelontinue
    else:
    m 6:
 
            
                   print("Sinue

# save
            
 
                          o0,romNumandint(0, num)
        else:
            continue
    
# if stai !
            
            tement to 
                check if num 
   is fluky
    if num =    print("Flukyse:
   7        sumFa break
    else:
        continue

# print total time and total loops
print(
    "Total Loops: " + str(
        loopCount))ctors += i

    #print(sumFactors)
 Number: " s+ str(randomNum))
       sumFactors = 1
    
    # find 1st generated randint of for seed value sumFactors
        elif sumFactors > 0: flukyCount += 1
        if flukyCount
